// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v6.33.4
// source: tyto/runtime/v1/guest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type ClientWritableStream,
  type handleBidiStreamingCall,
  type handleClientStreamingCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "tyto.runtime.v1";

export enum FileKind {
  FILE_KIND_UNSPECIFIED = 0,
  FILE_KIND_FILE = 1,
  FILE_KIND_DIRECTORY = 2,
  FILE_KIND_SYMLINK = 3,
  FILE_KIND_OTHER = 4,
}

export function fileKindFromJSON(object: any): FileKind {
  switch (object) {
    case 0:
    case "FILE_KIND_UNSPECIFIED":
      return FileKind.FILE_KIND_UNSPECIFIED;
    case 1:
    case "FILE_KIND_FILE":
      return FileKind.FILE_KIND_FILE;
    case 2:
    case "FILE_KIND_DIRECTORY":
      return FileKind.FILE_KIND_DIRECTORY;
    case 3:
    case "FILE_KIND_SYMLINK":
      return FileKind.FILE_KIND_SYMLINK;
    case 4:
    case "FILE_KIND_OTHER":
      return FileKind.FILE_KIND_OTHER;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FileKind");
  }
}

export function fileKindToJSON(object: FileKind): string {
  switch (object) {
    case FileKind.FILE_KIND_UNSPECIFIED:
      return "FILE_KIND_UNSPECIFIED";
    case FileKind.FILE_KIND_FILE:
      return "FILE_KIND_FILE";
    case FileKind.FILE_KIND_DIRECTORY:
      return "FILE_KIND_DIRECTORY";
    case FileKind.FILE_KIND_SYMLINK:
      return "FILE_KIND_SYMLINK";
    case FileKind.FILE_KIND_OTHER:
      return "FILE_KIND_OTHER";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FileKind");
  }
}

export enum SessionStatus {
  SESSION_STATUS_UNSPECIFIED = 0,
  SESSION_STATUS_STARTING = 1,
  SESSION_STATUS_IDLE = 2,
  SESSION_STATUS_ATTACHED = 3,
  SESSION_STATUS_EXITED = 4,
  SESSION_STATUS_KILLED = 5,
  SESSION_STATUS_FAILED = 6,
}

export function sessionStatusFromJSON(object: any): SessionStatus {
  switch (object) {
    case 0:
    case "SESSION_STATUS_UNSPECIFIED":
      return SessionStatus.SESSION_STATUS_UNSPECIFIED;
    case 1:
    case "SESSION_STATUS_STARTING":
      return SessionStatus.SESSION_STATUS_STARTING;
    case 2:
    case "SESSION_STATUS_IDLE":
      return SessionStatus.SESSION_STATUS_IDLE;
    case 3:
    case "SESSION_STATUS_ATTACHED":
      return SessionStatus.SESSION_STATUS_ATTACHED;
    case 4:
    case "SESSION_STATUS_EXITED":
      return SessionStatus.SESSION_STATUS_EXITED;
    case 5:
    case "SESSION_STATUS_KILLED":
      return SessionStatus.SESSION_STATUS_KILLED;
    case 6:
    case "SESSION_STATUS_FAILED":
      return SessionStatus.SESSION_STATUS_FAILED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum SessionStatus");
  }
}

export function sessionStatusToJSON(object: SessionStatus): string {
  switch (object) {
    case SessionStatus.SESSION_STATUS_UNSPECIFIED:
      return "SESSION_STATUS_UNSPECIFIED";
    case SessionStatus.SESSION_STATUS_STARTING:
      return "SESSION_STATUS_STARTING";
    case SessionStatus.SESSION_STATUS_IDLE:
      return "SESSION_STATUS_IDLE";
    case SessionStatus.SESSION_STATUS_ATTACHED:
      return "SESSION_STATUS_ATTACHED";
    case SessionStatus.SESSION_STATUS_EXITED:
      return "SESSION_STATUS_EXITED";
    case SessionStatus.SESSION_STATUS_KILLED:
      return "SESSION_STATUS_KILLED";
    case SessionStatus.SESSION_STATUS_FAILED:
      return "SESSION_STATUS_FAILED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum SessionStatus");
  }
}

export interface Fence {
  authorityGeneration: number;
  routeRevision: number;
}

export interface HealthRequest {
}

export interface HealthResponse {
  version: string;
  initialized: boolean;
  hostname: string;
}

export interface PostRestoreRequest {
  sandboxId: string;
  hostname: string;
  hostWallClockUnixNanos: number;
  freshEntropy: Buffer;
  fence:
    | Fence
    | undefined;
  /**
   * network, when present, is the journaled guest-network identity applied
   * inside the guest by one bounded netlink reconcile step. Absent for
   * templates and hosts without guest networking.
   */
  network: NetworkConfig | undefined;
}

/**
 * NetworkConfig is the exact guest NIC configuration to apply at PostRestore.
 * It carries identity and addressing only; the host owns everything outside the
 * guest (TAP, firewall, DNAT).
 */
export interface NetworkConfig {
  /** target MAC for the single guest NIC */
  mac: string;
  /** guest NIC MTU */
  mtu: number;
  /** guest IPv4 address */
  address: string;
  /** prefix length of the guest address */
  prefix: number;
  /** default-route gateway (the host side of the /30) */
  gateway: string;
}

export interface PostRestoreResponse {
  appliedFence:
    | Fence
    | undefined;
  /**
   * config_fingerprint echoes the exact network configuration Inizer applied,
   * so Nestled can require positive acknowledgement before recording CREATED.
   * Empty when no NetworkConfig was requested; an older Inizer that predates
   * this field always returns empty and cannot acknowledge a config.
   */
  configFingerprint: string;
}

export interface PrepareSuspendRequest {
  sandboxId: string;
  fence: Fence | undefined;
  minIdleDurationNanos: number;
}

export interface PrepareSuspendResponse {
  appliedFence:
    | Fence
    | undefined;
  /**
   * sessions is the managed-session registry snapshot captured after this
   * Inizer drains, with a final lazy-reap pass so it never reports a dead
   * process as running. Nestled journals this snapshot and republishes it
   * on the SUSPENDING/SUSPENDED route update (see Route.session_snapshot in
   * the internal Nestled-to-Roxy wire contract), so Roxy can answer
   * ListSessions on a suspended sandbox without waking it. Metadata only;
   * replay bytes never leave the guest. Empty for an Inizer built before
   * Sprint 14.
   */
  sessions: SessionInfo[];
}

export interface AbortSuspendRequest {
  sandboxId: string;
  fence: Fence | undefined;
}

export interface AbortSuspendResponse {
  appliedFence: Fence | undefined;
}

export interface PrepareResumeRequest {
  sandboxId: string;
  hostWallClockUnixNanos: number;
  freshEntropy: Buffer;
  fence: Fence | undefined;
}

export interface PrepareResumeResponse {
  appliedFence: Fence | undefined;
}

export interface FreezeFilesystemRequest {
  sandboxId: string;
  snapshotId: string;
  fence: Fence | undefined;
}

export interface FreezeFilesystemResponse {
  appliedFence: Fence | undefined;
}

export interface ThawFilesystemRequest {
  sandboxId: string;
  snapshotId: string;
  fence: Fence | undefined;
}

export interface ThawFilesystemResponse {
  appliedFence: Fence | undefined;
}

export interface ReadFileRequest {
  sandboxId: string;
  path: string;
}

export interface ReadFileResponse {
  data: Buffer;
}

export interface WriteFileRequest {
  start?: WriteFileStart | undefined;
  chunk?: WriteFileChunk | undefined;
}

export interface WriteFileStart {
  sandboxId: string;
  path: string;
}

export interface WriteFileChunk {
  data: Buffer;
}

export interface WriteFileResponse {
  bytesWritten: number;
}

export interface ListDirectoryRequest {
  sandboxId: string;
  path: string;
}

export interface ListDirectoryResponse {
  file: FileInfo | undefined;
}

export interface StatFileRequest {
  sandboxId: string;
  path: string;
}

export interface StatFileResponse {
  file: FileInfo | undefined;
}

export interface FileInfo {
  path: string;
  name: string;
  kind: FileKind;
  size: number;
  mode: number;
  modifiedAtUnixNanos: number;
}

export interface MakeDirectoryRequest {
  sandboxId: string;
  path: string;
}

export interface MakeDirectoryResponse {
}

export interface RemoveFileRequest {
  sandboxId: string;
  path: string;
  recursive: boolean;
}

export interface RemoveFileResponse {
}

export interface MoveFileRequest {
  sandboxId: string;
  sourcePath: string;
  destinationPath: string;
}

export interface MoveFileResponse {
}

export interface ExecRequest {
  start?: ExecStart | undefined;
  stdin?: StdinData | undefined;
  cancel?: ExecCancel | undefined;
  resize?: ExecResize | undefined;
}

export interface ExecResponse {
  stdout?: StdoutData | undefined;
  stderr?: StderrData | undefined;
  exit?: ExecExit | undefined;
}

export interface ExecStart {
  command: string[];
  env: { [key: string]: string };
  workingDir: string;
  tty: boolean;
  cols: number;
  rows: number;
}

export interface ExecStart_EnvEntry {
  key: string;
  value: string;
}

export interface StdinData {
  data: Buffer;
}

export interface StdoutData {
  data: Buffer;
}

export interface StderrData {
  data: Buffer;
}

export interface ExecCancel {
}

export interface ExecResize {
  cols: number;
  rows: number;
}

export interface ExecExit {
  exitCode: number;
  signaled: boolean;
  signal: number;
}

export interface SessionInfo {
  name: string;
  command: string[];
  workingDir: string;
  status: SessionStatus;
  attached: boolean;
  startedAtUnixNanos: number;
  lastActivityUnixNanos: number;
  /** ended_at_unix_nanos is 0 while the session is running. */
  endedAtUnixNanos: number;
  /** exit is set only once the session has reached a terminal status. */
  exit: ExecExit | undefined;
}

export interface CreateSessionRequest {
  /**
   * name is the session's identity. Required.
   * Charset: ^[a-z][a-z0-9-]{0,31}$ (deliberately admits "console" as a
   * reserved future name; see plan D15).
   */
  name: string;
  command: string[];
  env: { [key: string]: string };
  workingDir: string;
  /** cols/rows: 0 defaults to 80x24; max 512 (same bounds as ExecStart). */
  cols: number;
  rows: number;
  /**
   * replace, if true, replaces an existing EXITED/KILLED/FAILED record
   * (and discards its replay buffer) instead of returning ALREADY_EXISTS.
   * A running or attached session is never replaceable regardless of this
   * flag — kill it first. See plan D1/D3 (review finding F3).
   */
  replace: boolean;
}

export interface CreateSessionRequest_EnvEntry {
  key: string;
  value: string;
}

export interface CreateSessionResponse {
  session: SessionInfo | undefined;
}

export interface ListSessionsRequest {
}

export interface ListSessionsResponse {
  sessions: SessionInfo[];
  /**
   * sandbox_suspended is true when this response was served from the
   * suspend-time snapshot rather than the live guest registry (Roxy, on a
   * SUSPENDING/SUSPENDED/RESUMING route). Always false for a response
   * Inizer produces directly, since Inizer is only reachable while running.
   */
  sandboxSuspended: boolean;
}

export interface KillSessionRequest {
  name: string;
  /** signal defaults to "TERM" when empty. */
  signal: string;
  /**
   * grace_ms defaults to 5000 when 0: signal, wait, then SIGKILL the
   * process group if it has not exited.
   */
  graceMs: number;
}

export interface KillSessionResponse {
  session: SessionInfo | undefined;
}

export interface AttachSessionRequest {
  start?: AttachStart | undefined;
  stdin?: StdinData | undefined;
  resize?: ExecResize | undefined;
  detach?: AttachDetach | undefined;
  signal?: SessionSignal | undefined;
}

export interface AttachStart {
  name: string;
  /**
   * cols/rows applied at attach time; 0 keeps the session's current
   * dimensions rather than resetting to a default.
   */
  cols: number;
  rows: number;
  /**
   * max_replay_bytes bounds how much of the replay ring is sent on accept;
   * 0 or a value exceeding the ring capacity (1 MiB) sends the full ring.
   */
  maxReplayBytes: number;
}

/**
 * AttachDetach is a graceful client-initiated detach. The session process
 * is not signaled.
 */
export interface AttachDetach {
}

/**
 * SessionSignal forwards a signal to the process group without ending the
 * attach stream (distinct from KillSessionRequest, which is a standalone
 * call and does end any current attach implicitly once the process dies).
 */
export interface SessionSignal {
  signal: string;
}

export interface AttachSessionResponse {
  accepted?:
    | AttachAccepted
    | undefined;
  /**
   * output is reused StdoutData: merged TTY bytes, replay then live.
   * StderrData never appears on a session attach stream (TTY-only).
   */
  output?: StdoutData | undefined;
  exit?: ExecExit | undefined;
  ended?:
    | AttachEnded
    | undefined;
  /**
   * output_dropped notifies an attached client that live output was
   * lost to backpressure (plan D4: a slow attached client never blocks
   * the guest's PTY reader, so excess bytes are dropped instead).
   * Without this, a slow-but-still-attached client has no way to know
   * its terminal view is now missing bytes. Distinct from
   * AttachAccepted.history_dropped, which reports replay-ring
   * truncation measured once, before this attach's live output begins;
   * this can arrive repeatedly during one attach. The attach stream is
   * NOT ended by this — dropping continues to be preferred over
   * blocking or disconnecting, per D4 and invariant S4 — the client
   * decides whether to detach/reattach on a persistent gap.
   */
  outputDropped?: OutputDropped | undefined;
}

export interface OutputDropped {
  /**
   * dropped_bytes accumulates every byte dropped since the last
   * OutputDropped frame (or since attach, for the first one). Coalesced
   * and delivered opportunistically the next time the output channel has
   * room, rather than contending for space on that same congested
   * channel — a live counter, not a per-drop event.
   */
  droppedBytes: number;
}

export interface AttachAccepted {
  session: SessionInfo | undefined;
  replayedBytes: number;
  /**
   * history_dropped is true if the ring has dropped bytes since the
   * session started, i.e. the replay sent is known-incomplete.
   */
  historyDropped: boolean;
  /**
   * cols/rows actually applied (may differ from the request if the
   * session was already attached at different dimensions and the
   * request's cols/rows were both 0).
   */
  cols: number;
  rows: number;
}

export interface AttachEnded {
  reason: AttachEnded_Reason;
}

export enum AttachEnded_Reason {
  REASON_UNSPECIFIED = 0,
  /**
   * REASON_DETACHED - The client sent AttachDetach, or the connection was lost (including
   * a silently-dead client past Roxy's keepalive bound).
   */
  REASON_DETACHED = 1,
  /**
   * REASON_TAKEOVER - A newer AttachSession call took over this session; this stream
   * always loses to the new one (see plan D5 — takeover, not rejection).
   */
  REASON_TAKEOVER = 2,
}

export function attachEnded_ReasonFromJSON(object: any): AttachEnded_Reason {
  switch (object) {
    case 0:
    case "REASON_UNSPECIFIED":
      return AttachEnded_Reason.REASON_UNSPECIFIED;
    case 1:
    case "REASON_DETACHED":
      return AttachEnded_Reason.REASON_DETACHED;
    case 2:
    case "REASON_TAKEOVER":
      return AttachEnded_Reason.REASON_TAKEOVER;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum AttachEnded_Reason");
  }
}

export function attachEnded_ReasonToJSON(object: AttachEnded_Reason): string {
  switch (object) {
    case AttachEnded_Reason.REASON_UNSPECIFIED:
      return "REASON_UNSPECIFIED";
    case AttachEnded_Reason.REASON_DETACHED:
      return "REASON_DETACHED";
    case AttachEnded_Reason.REASON_TAKEOVER:
      return "REASON_TAKEOVER";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum AttachEnded_Reason");
  }
}

function createBaseFence(): Fence {
  return { authorityGeneration: 0, routeRevision: 0 };
}

export const Fence: MessageFns<Fence> = {
  encode(message: Fence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authorityGeneration !== 0) {
      writer.uint32(8).uint64(message.authorityGeneration);
    }
    if (message.routeRevision !== 0) {
      writer.uint32(16).uint64(message.routeRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.authorityGeneration = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.routeRevision = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fence {
    return {
      authorityGeneration: isSet(object.authorityGeneration)
        ? globalThis.Number(object.authorityGeneration)
        : isSet(object.authority_generation)
        ? globalThis.Number(object.authority_generation)
        : 0,
      routeRevision: isSet(object.routeRevision)
        ? globalThis.Number(object.routeRevision)
        : isSet(object.route_revision)
        ? globalThis.Number(object.route_revision)
        : 0,
    };
  },

  toJSON(message: Fence): unknown {
    const obj: any = {};
    if (message.authorityGeneration !== 0) {
      obj.authorityGeneration = Math.round(message.authorityGeneration);
    }
    if (message.routeRevision !== 0) {
      obj.routeRevision = Math.round(message.routeRevision);
    }
    return obj;
  },

  create(base?: DeepPartial<Fence>): Fence {
    return Fence.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fence>): Fence {
    const message = createBaseFence();
    message.authorityGeneration = object.authorityGeneration ?? 0;
    message.routeRevision = object.routeRevision ?? 0;
    return message;
  },
};

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { version: "", initialized: false, hostname: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.initialized !== false) {
      writer.uint32(16).bool(message.initialized);
    }
    if (message.hostname !== "") {
      writer.uint32(26).string(message.hostname);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.initialized = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      initialized: isSet(object.initialized) ? globalThis.Boolean(object.initialized) : false,
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.initialized !== false) {
      obj.initialized = message.initialized;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthResponse>): HealthResponse {
    return HealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthResponse>): HealthResponse {
    const message = createBaseHealthResponse();
    message.version = object.version ?? "";
    message.initialized = object.initialized ?? false;
    message.hostname = object.hostname ?? "";
    return message;
  },
};

function createBasePostRestoreRequest(): PostRestoreRequest {
  return {
    sandboxId: "",
    hostname: "",
    hostWallClockUnixNanos: 0,
    freshEntropy: Buffer.alloc(0),
    fence: undefined,
    network: undefined,
  };
}

export const PostRestoreRequest: MessageFns<PostRestoreRequest> = {
  encode(message: PostRestoreRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.hostname !== "") {
      writer.uint32(18).string(message.hostname);
    }
    if (message.hostWallClockUnixNanos !== 0) {
      writer.uint32(24).int64(message.hostWallClockUnixNanos);
    }
    if (message.freshEntropy.length !== 0) {
      writer.uint32(34).bytes(message.freshEntropy);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(42).fork()).join();
    }
    if (message.network !== undefined) {
      NetworkConfig.encode(message.network, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PostRestoreRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePostRestoreRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hostWallClockUnixNanos = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.freshEntropy = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.network = NetworkConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PostRestoreRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      hostWallClockUnixNanos: isSet(object.hostWallClockUnixNanos)
        ? globalThis.Number(object.hostWallClockUnixNanos)
        : isSet(object.host_wall_clock_unix_nanos)
        ? globalThis.Number(object.host_wall_clock_unix_nanos)
        : 0,
      freshEntropy: isSet(object.freshEntropy)
        ? Buffer.from(bytesFromBase64(object.freshEntropy))
        : isSet(object.fresh_entropy)
        ? Buffer.from(bytesFromBase64(object.fresh_entropy))
        : Buffer.alloc(0),
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
      network: isSet(object.network) ? NetworkConfig.fromJSON(object.network) : undefined,
    };
  },

  toJSON(message: PostRestoreRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.hostWallClockUnixNanos !== 0) {
      obj.hostWallClockUnixNanos = Math.round(message.hostWallClockUnixNanos);
    }
    if (message.freshEntropy.length !== 0) {
      obj.freshEntropy = base64FromBytes(message.freshEntropy);
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    if (message.network !== undefined) {
      obj.network = NetworkConfig.toJSON(message.network);
    }
    return obj;
  },

  create(base?: DeepPartial<PostRestoreRequest>): PostRestoreRequest {
    return PostRestoreRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PostRestoreRequest>): PostRestoreRequest {
    const message = createBasePostRestoreRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.hostname = object.hostname ?? "";
    message.hostWallClockUnixNanos = object.hostWallClockUnixNanos ?? 0;
    message.freshEntropy = object.freshEntropy ?? Buffer.alloc(0);
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    message.network = (object.network !== undefined && object.network !== null)
      ? NetworkConfig.fromPartial(object.network)
      : undefined;
    return message;
  },
};

function createBaseNetworkConfig(): NetworkConfig {
  return { mac: "", mtu: 0, address: "", prefix: 0, gateway: "" };
}

export const NetworkConfig: MessageFns<NetworkConfig> = {
  encode(message: NetworkConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mac !== "") {
      writer.uint32(10).string(message.mac);
    }
    if (message.mtu !== 0) {
      writer.uint32(16).uint32(message.mtu);
    }
    if (message.address !== "") {
      writer.uint32(26).string(message.address);
    }
    if (message.prefix !== 0) {
      writer.uint32(32).uint32(message.prefix);
    }
    if (message.gateway !== "") {
      writer.uint32(42).string(message.gateway);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mac = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mtu = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.prefix = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gateway = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkConfig {
    return {
      mac: isSet(object.mac) ? globalThis.String(object.mac) : "",
      mtu: isSet(object.mtu) ? globalThis.Number(object.mtu) : 0,
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      prefix: isSet(object.prefix) ? globalThis.Number(object.prefix) : 0,
      gateway: isSet(object.gateway) ? globalThis.String(object.gateway) : "",
    };
  },

  toJSON(message: NetworkConfig): unknown {
    const obj: any = {};
    if (message.mac !== "") {
      obj.mac = message.mac;
    }
    if (message.mtu !== 0) {
      obj.mtu = Math.round(message.mtu);
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.prefix !== 0) {
      obj.prefix = Math.round(message.prefix);
    }
    if (message.gateway !== "") {
      obj.gateway = message.gateway;
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkConfig>): NetworkConfig {
    return NetworkConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkConfig>): NetworkConfig {
    const message = createBaseNetworkConfig();
    message.mac = object.mac ?? "";
    message.mtu = object.mtu ?? 0;
    message.address = object.address ?? "";
    message.prefix = object.prefix ?? 0;
    message.gateway = object.gateway ?? "";
    return message;
  },
};

function createBasePostRestoreResponse(): PostRestoreResponse {
  return { appliedFence: undefined, configFingerprint: "" };
}

export const PostRestoreResponse: MessageFns<PostRestoreResponse> = {
  encode(message: PostRestoreResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    if (message.configFingerprint !== "") {
      writer.uint32(18).string(message.configFingerprint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PostRestoreResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePostRestoreResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.configFingerprint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PostRestoreResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
      configFingerprint: isSet(object.configFingerprint)
        ? globalThis.String(object.configFingerprint)
        : isSet(object.config_fingerprint)
        ? globalThis.String(object.config_fingerprint)
        : "",
    };
  },

  toJSON(message: PostRestoreResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    if (message.configFingerprint !== "") {
      obj.configFingerprint = message.configFingerprint;
    }
    return obj;
  },

  create(base?: DeepPartial<PostRestoreResponse>): PostRestoreResponse {
    return PostRestoreResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PostRestoreResponse>): PostRestoreResponse {
    const message = createBasePostRestoreResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    message.configFingerprint = object.configFingerprint ?? "";
    return message;
  },
};

function createBasePrepareSuspendRequest(): PrepareSuspendRequest {
  return { sandboxId: "", fence: undefined, minIdleDurationNanos: 0 };
}

export const PrepareSuspendRequest: MessageFns<PrepareSuspendRequest> = {
  encode(message: PrepareSuspendRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(18).fork()).join();
    }
    if (message.minIdleDurationNanos !== 0) {
      writer.uint32(24).int64(message.minIdleDurationNanos);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareSuspendRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareSuspendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minIdleDurationNanos = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareSuspendRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
      minIdleDurationNanos: isSet(object.minIdleDurationNanos)
        ? globalThis.Number(object.minIdleDurationNanos)
        : isSet(object.min_idle_duration_nanos)
        ? globalThis.Number(object.min_idle_duration_nanos)
        : 0,
    };
  },

  toJSON(message: PrepareSuspendRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    if (message.minIdleDurationNanos !== 0) {
      obj.minIdleDurationNanos = Math.round(message.minIdleDurationNanos);
    }
    return obj;
  },

  create(base?: DeepPartial<PrepareSuspendRequest>): PrepareSuspendRequest {
    return PrepareSuspendRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PrepareSuspendRequest>): PrepareSuspendRequest {
    const message = createBasePrepareSuspendRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    message.minIdleDurationNanos = object.minIdleDurationNanos ?? 0;
    return message;
  },
};

function createBasePrepareSuspendResponse(): PrepareSuspendResponse {
  return { appliedFence: undefined, sessions: [] };
}

export const PrepareSuspendResponse: MessageFns<PrepareSuspendResponse> = {
  encode(message: PrepareSuspendResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    for (const v of message.sessions) {
      SessionInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareSuspendResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareSuspendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessions.push(SessionInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareSuspendResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => SessionInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PrepareSuspendResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => SessionInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PrepareSuspendResponse>): PrepareSuspendResponse {
    return PrepareSuspendResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PrepareSuspendResponse>): PrepareSuspendResponse {
    const message = createBasePrepareSuspendResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    message.sessions = object.sessions?.map((e) => SessionInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAbortSuspendRequest(): AbortSuspendRequest {
  return { sandboxId: "", fence: undefined };
}

export const AbortSuspendRequest: MessageFns<AbortSuspendRequest> = {
  encode(message: AbortSuspendRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AbortSuspendRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAbortSuspendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AbortSuspendRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
    };
  },

  toJSON(message: AbortSuspendRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    return obj;
  },

  create(base?: DeepPartial<AbortSuspendRequest>): AbortSuspendRequest {
    return AbortSuspendRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AbortSuspendRequest>): AbortSuspendRequest {
    const message = createBaseAbortSuspendRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    return message;
  },
};

function createBaseAbortSuspendResponse(): AbortSuspendResponse {
  return { appliedFence: undefined };
}

export const AbortSuspendResponse: MessageFns<AbortSuspendResponse> = {
  encode(message: AbortSuspendResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AbortSuspendResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAbortSuspendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AbortSuspendResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
    };
  },

  toJSON(message: AbortSuspendResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    return obj;
  },

  create(base?: DeepPartial<AbortSuspendResponse>): AbortSuspendResponse {
    return AbortSuspendResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AbortSuspendResponse>): AbortSuspendResponse {
    const message = createBaseAbortSuspendResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    return message;
  },
};

function createBasePrepareResumeRequest(): PrepareResumeRequest {
  return { sandboxId: "", hostWallClockUnixNanos: 0, freshEntropy: Buffer.alloc(0), fence: undefined };
}

export const PrepareResumeRequest: MessageFns<PrepareResumeRequest> = {
  encode(message: PrepareResumeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.hostWallClockUnixNanos !== 0) {
      writer.uint32(16).int64(message.hostWallClockUnixNanos);
    }
    if (message.freshEntropy.length !== 0) {
      writer.uint32(26).bytes(message.freshEntropy);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareResumeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareResumeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hostWallClockUnixNanos = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.freshEntropy = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareResumeRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      hostWallClockUnixNanos: isSet(object.hostWallClockUnixNanos)
        ? globalThis.Number(object.hostWallClockUnixNanos)
        : isSet(object.host_wall_clock_unix_nanos)
        ? globalThis.Number(object.host_wall_clock_unix_nanos)
        : 0,
      freshEntropy: isSet(object.freshEntropy)
        ? Buffer.from(bytesFromBase64(object.freshEntropy))
        : isSet(object.fresh_entropy)
        ? Buffer.from(bytesFromBase64(object.fresh_entropy))
        : Buffer.alloc(0),
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
    };
  },

  toJSON(message: PrepareResumeRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.hostWallClockUnixNanos !== 0) {
      obj.hostWallClockUnixNanos = Math.round(message.hostWallClockUnixNanos);
    }
    if (message.freshEntropy.length !== 0) {
      obj.freshEntropy = base64FromBytes(message.freshEntropy);
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    return obj;
  },

  create(base?: DeepPartial<PrepareResumeRequest>): PrepareResumeRequest {
    return PrepareResumeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PrepareResumeRequest>): PrepareResumeRequest {
    const message = createBasePrepareResumeRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.hostWallClockUnixNanos = object.hostWallClockUnixNanos ?? 0;
    message.freshEntropy = object.freshEntropy ?? Buffer.alloc(0);
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    return message;
  },
};

function createBasePrepareResumeResponse(): PrepareResumeResponse {
  return { appliedFence: undefined };
}

export const PrepareResumeResponse: MessageFns<PrepareResumeResponse> = {
  encode(message: PrepareResumeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrepareResumeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrepareResumeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrepareResumeResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
    };
  },

  toJSON(message: PrepareResumeResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    return obj;
  },

  create(base?: DeepPartial<PrepareResumeResponse>): PrepareResumeResponse {
    return PrepareResumeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PrepareResumeResponse>): PrepareResumeResponse {
    const message = createBasePrepareResumeResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    return message;
  },
};

function createBaseFreezeFilesystemRequest(): FreezeFilesystemRequest {
  return { sandboxId: "", snapshotId: "", fence: undefined };
}

export const FreezeFilesystemRequest: MessageFns<FreezeFilesystemRequest> = {
  encode(message: FreezeFilesystemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.snapshotId !== "") {
      writer.uint32(18).string(message.snapshotId);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FreezeFilesystemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreezeFilesystemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.snapshotId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FreezeFilesystemRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      snapshotId: isSet(object.snapshotId)
        ? globalThis.String(object.snapshotId)
        : isSet(object.snapshot_id)
        ? globalThis.String(object.snapshot_id)
        : "",
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
    };
  },

  toJSON(message: FreezeFilesystemRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.snapshotId !== "") {
      obj.snapshotId = message.snapshotId;
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    return obj;
  },

  create(base?: DeepPartial<FreezeFilesystemRequest>): FreezeFilesystemRequest {
    return FreezeFilesystemRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FreezeFilesystemRequest>): FreezeFilesystemRequest {
    const message = createBaseFreezeFilesystemRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.snapshotId = object.snapshotId ?? "";
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    return message;
  },
};

function createBaseFreezeFilesystemResponse(): FreezeFilesystemResponse {
  return { appliedFence: undefined };
}

export const FreezeFilesystemResponse: MessageFns<FreezeFilesystemResponse> = {
  encode(message: FreezeFilesystemResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FreezeFilesystemResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreezeFilesystemResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FreezeFilesystemResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
    };
  },

  toJSON(message: FreezeFilesystemResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    return obj;
  },

  create(base?: DeepPartial<FreezeFilesystemResponse>): FreezeFilesystemResponse {
    return FreezeFilesystemResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FreezeFilesystemResponse>): FreezeFilesystemResponse {
    const message = createBaseFreezeFilesystemResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    return message;
  },
};

function createBaseThawFilesystemRequest(): ThawFilesystemRequest {
  return { sandboxId: "", snapshotId: "", fence: undefined };
}

export const ThawFilesystemRequest: MessageFns<ThawFilesystemRequest> = {
  encode(message: ThawFilesystemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.snapshotId !== "") {
      writer.uint32(18).string(message.snapshotId);
    }
    if (message.fence !== undefined) {
      Fence.encode(message.fence, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThawFilesystemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThawFilesystemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.snapshotId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThawFilesystemRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      snapshotId: isSet(object.snapshotId)
        ? globalThis.String(object.snapshotId)
        : isSet(object.snapshot_id)
        ? globalThis.String(object.snapshot_id)
        : "",
      fence: isSet(object.fence) ? Fence.fromJSON(object.fence) : undefined,
    };
  },

  toJSON(message: ThawFilesystemRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.snapshotId !== "") {
      obj.snapshotId = message.snapshotId;
    }
    if (message.fence !== undefined) {
      obj.fence = Fence.toJSON(message.fence);
    }
    return obj;
  },

  create(base?: DeepPartial<ThawFilesystemRequest>): ThawFilesystemRequest {
    return ThawFilesystemRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThawFilesystemRequest>): ThawFilesystemRequest {
    const message = createBaseThawFilesystemRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.snapshotId = object.snapshotId ?? "";
    message.fence = (object.fence !== undefined && object.fence !== null) ? Fence.fromPartial(object.fence) : undefined;
    return message;
  },
};

function createBaseThawFilesystemResponse(): ThawFilesystemResponse {
  return { appliedFence: undefined };
}

export const ThawFilesystemResponse: MessageFns<ThawFilesystemResponse> = {
  encode(message: ThawFilesystemResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appliedFence !== undefined) {
      Fence.encode(message.appliedFence, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThawFilesystemResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThawFilesystemResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appliedFence = Fence.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThawFilesystemResponse {
    return {
      appliedFence: isSet(object.appliedFence)
        ? Fence.fromJSON(object.appliedFence)
        : isSet(object.applied_fence)
        ? Fence.fromJSON(object.applied_fence)
        : undefined,
    };
  },

  toJSON(message: ThawFilesystemResponse): unknown {
    const obj: any = {};
    if (message.appliedFence !== undefined) {
      obj.appliedFence = Fence.toJSON(message.appliedFence);
    }
    return obj;
  },

  create(base?: DeepPartial<ThawFilesystemResponse>): ThawFilesystemResponse {
    return ThawFilesystemResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThawFilesystemResponse>): ThawFilesystemResponse {
    const message = createBaseThawFilesystemResponse();
    message.appliedFence = (object.appliedFence !== undefined && object.appliedFence !== null)
      ? Fence.fromPartial(object.appliedFence)
      : undefined;
    return message;
  },
};

function createBaseReadFileRequest(): ReadFileRequest {
  return { sandboxId: "", path: "" };
}

export const ReadFileRequest: MessageFns<ReadFileRequest> = {
  encode(message: ReadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadFileRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: ReadFileRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadFileRequest>): ReadFileRequest {
    return ReadFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadFileRequest>): ReadFileRequest {
    const message = createBaseReadFileRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseReadFileResponse(): ReadFileResponse {
  return { data: Buffer.alloc(0) };
}

export const ReadFileResponse: MessageFns<ReadFileResponse> = {
  encode(message: ReadFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadFileResponse {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: ReadFileResponse): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<ReadFileResponse>): ReadFileResponse {
    return ReadFileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadFileResponse>): ReadFileResponse {
    const message = createBaseReadFileResponse();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWriteFileRequest(): WriteFileRequest {
  return { start: undefined, chunk: undefined };
}

export const WriteFileRequest: MessageFns<WriteFileRequest> = {
  encode(message: WriteFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      WriteFileStart.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.chunk !== undefined) {
      WriteFileChunk.encode(message.chunk, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = WriteFileStart.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunk = WriteFileChunk.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteFileRequest {
    return {
      start: isSet(object.start) ? WriteFileStart.fromJSON(object.start) : undefined,
      chunk: isSet(object.chunk) ? WriteFileChunk.fromJSON(object.chunk) : undefined,
    };
  },

  toJSON(message: WriteFileRequest): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = WriteFileStart.toJSON(message.start);
    }
    if (message.chunk !== undefined) {
      obj.chunk = WriteFileChunk.toJSON(message.chunk);
    }
    return obj;
  },

  create(base?: DeepPartial<WriteFileRequest>): WriteFileRequest {
    return WriteFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteFileRequest>): WriteFileRequest {
    const message = createBaseWriteFileRequest();
    message.start = (object.start !== undefined && object.start !== null)
      ? WriteFileStart.fromPartial(object.start)
      : undefined;
    message.chunk = (object.chunk !== undefined && object.chunk !== null)
      ? WriteFileChunk.fromPartial(object.chunk)
      : undefined;
    return message;
  },
};

function createBaseWriteFileStart(): WriteFileStart {
  return { sandboxId: "", path: "" };
}

export const WriteFileStart: MessageFns<WriteFileStart> = {
  encode(message: WriteFileStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteFileStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteFileStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteFileStart {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: WriteFileStart): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<WriteFileStart>): WriteFileStart {
    return WriteFileStart.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteFileStart>): WriteFileStart {
    const message = createBaseWriteFileStart();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseWriteFileChunk(): WriteFileChunk {
  return { data: Buffer.alloc(0) };
}

export const WriteFileChunk: MessageFns<WriteFileChunk> = {
  encode(message: WriteFileChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteFileChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteFileChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteFileChunk {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: WriteFileChunk): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<WriteFileChunk>): WriteFileChunk {
    return WriteFileChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteFileChunk>): WriteFileChunk {
    const message = createBaseWriteFileChunk();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWriteFileResponse(): WriteFileResponse {
  return { bytesWritten: 0 };
}

export const WriteFileResponse: MessageFns<WriteFileResponse> = {
  encode(message: WriteFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bytesWritten !== 0) {
      writer.uint32(8).uint64(message.bytesWritten);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.bytesWritten = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteFileResponse {
    return {
      bytesWritten: isSet(object.bytesWritten)
        ? globalThis.Number(object.bytesWritten)
        : isSet(object.bytes_written)
        ? globalThis.Number(object.bytes_written)
        : 0,
    };
  },

  toJSON(message: WriteFileResponse): unknown {
    const obj: any = {};
    if (message.bytesWritten !== 0) {
      obj.bytesWritten = Math.round(message.bytesWritten);
    }
    return obj;
  },

  create(base?: DeepPartial<WriteFileResponse>): WriteFileResponse {
    return WriteFileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteFileResponse>): WriteFileResponse {
    const message = createBaseWriteFileResponse();
    message.bytesWritten = object.bytesWritten ?? 0;
    return message;
  },
};

function createBaseListDirectoryRequest(): ListDirectoryRequest {
  return { sandboxId: "", path: "" };
}

export const ListDirectoryRequest: MessageFns<ListDirectoryRequest> = {
  encode(message: ListDirectoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDirectoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDirectoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDirectoryRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: ListDirectoryRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<ListDirectoryRequest>): ListDirectoryRequest {
    return ListDirectoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDirectoryRequest>): ListDirectoryRequest {
    const message = createBaseListDirectoryRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseListDirectoryResponse(): ListDirectoryResponse {
  return { file: undefined };
}

export const ListDirectoryResponse: MessageFns<ListDirectoryResponse> = {
  encode(message: ListDirectoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      FileInfo.encode(message.file, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDirectoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDirectoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = FileInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDirectoryResponse {
    return { file: isSet(object.file) ? FileInfo.fromJSON(object.file) : undefined };
  },

  toJSON(message: ListDirectoryResponse): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = FileInfo.toJSON(message.file);
    }
    return obj;
  },

  create(base?: DeepPartial<ListDirectoryResponse>): ListDirectoryResponse {
    return ListDirectoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDirectoryResponse>): ListDirectoryResponse {
    const message = createBaseListDirectoryResponse();
    message.file = (object.file !== undefined && object.file !== null) ? FileInfo.fromPartial(object.file) : undefined;
    return message;
  },
};

function createBaseStatFileRequest(): StatFileRequest {
  return { sandboxId: "", path: "" };
}

export const StatFileRequest: MessageFns<StatFileRequest> = {
  encode(message: StatFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatFileRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: StatFileRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<StatFileRequest>): StatFileRequest {
    return StatFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatFileRequest>): StatFileRequest {
    const message = createBaseStatFileRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseStatFileResponse(): StatFileResponse {
  return { file: undefined };
}

export const StatFileResponse: MessageFns<StatFileResponse> = {
  encode(message: StatFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file !== undefined) {
      FileInfo.encode(message.file, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file = FileInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatFileResponse {
    return { file: isSet(object.file) ? FileInfo.fromJSON(object.file) : undefined };
  },

  toJSON(message: StatFileResponse): unknown {
    const obj: any = {};
    if (message.file !== undefined) {
      obj.file = FileInfo.toJSON(message.file);
    }
    return obj;
  },

  create(base?: DeepPartial<StatFileResponse>): StatFileResponse {
    return StatFileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatFileResponse>): StatFileResponse {
    const message = createBaseStatFileResponse();
    message.file = (object.file !== undefined && object.file !== null) ? FileInfo.fromPartial(object.file) : undefined;
    return message;
  },
};

function createBaseFileInfo(): FileInfo {
  return { path: "", name: "", kind: 0, size: 0, mode: 0, modifiedAtUnixNanos: 0 };
}

export const FileInfo: MessageFns<FileInfo> = {
  encode(message: FileInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.size !== 0) {
      writer.uint32(32).uint64(message.size);
    }
    if (message.mode !== 0) {
      writer.uint32(40).uint32(message.mode);
    }
    if (message.modifiedAtUnixNanos !== 0) {
      writer.uint32(48).int64(message.modifiedAtUnixNanos);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.mode = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.modifiedAtUnixNanos = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileInfo {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      kind: isSet(object.kind) ? fileKindFromJSON(object.kind) : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      mode: isSet(object.mode) ? globalThis.Number(object.mode) : 0,
      modifiedAtUnixNanos: isSet(object.modifiedAtUnixNanos)
        ? globalThis.Number(object.modifiedAtUnixNanos)
        : isSet(object.modified_at_unix_nanos)
        ? globalThis.Number(object.modified_at_unix_nanos)
        : 0,
    };
  },

  toJSON(message: FileInfo): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.kind !== 0) {
      obj.kind = fileKindToJSON(message.kind);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.mode !== 0) {
      obj.mode = Math.round(message.mode);
    }
    if (message.modifiedAtUnixNanos !== 0) {
      obj.modifiedAtUnixNanos = Math.round(message.modifiedAtUnixNanos);
    }
    return obj;
  },

  create(base?: DeepPartial<FileInfo>): FileInfo {
    return FileInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FileInfo>): FileInfo {
    const message = createBaseFileInfo();
    message.path = object.path ?? "";
    message.name = object.name ?? "";
    message.kind = object.kind ?? 0;
    message.size = object.size ?? 0;
    message.mode = object.mode ?? 0;
    message.modifiedAtUnixNanos = object.modifiedAtUnixNanos ?? 0;
    return message;
  },
};

function createBaseMakeDirectoryRequest(): MakeDirectoryRequest {
  return { sandboxId: "", path: "" };
}

export const MakeDirectoryRequest: MessageFns<MakeDirectoryRequest> = {
  encode(message: MakeDirectoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MakeDirectoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMakeDirectoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MakeDirectoryRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
    };
  },

  toJSON(message: MakeDirectoryRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<MakeDirectoryRequest>): MakeDirectoryRequest {
    return MakeDirectoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MakeDirectoryRequest>): MakeDirectoryRequest {
    const message = createBaseMakeDirectoryRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseMakeDirectoryResponse(): MakeDirectoryResponse {
  return {};
}

export const MakeDirectoryResponse: MessageFns<MakeDirectoryResponse> = {
  encode(_: MakeDirectoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MakeDirectoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMakeDirectoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MakeDirectoryResponse {
    return {};
  },

  toJSON(_: MakeDirectoryResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MakeDirectoryResponse>): MakeDirectoryResponse {
    return MakeDirectoryResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MakeDirectoryResponse>): MakeDirectoryResponse {
    const message = createBaseMakeDirectoryResponse();
    return message;
  },
};

function createBaseRemoveFileRequest(): RemoveFileRequest {
  return { sandboxId: "", path: "", recursive: false };
}

export const RemoveFileRequest: MessageFns<RemoveFileRequest> = {
  encode(message: RemoveFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.recursive !== false) {
      writer.uint32(24).bool(message.recursive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveFileRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
    };
  },

  toJSON(message: RemoveFileRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveFileRequest>): RemoveFileRequest {
    return RemoveFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveFileRequest>): RemoveFileRequest {
    const message = createBaseRemoveFileRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.path = object.path ?? "";
    message.recursive = object.recursive ?? false;
    return message;
  },
};

function createBaseRemoveFileResponse(): RemoveFileResponse {
  return {};
}

export const RemoveFileResponse: MessageFns<RemoveFileResponse> = {
  encode(_: RemoveFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RemoveFileResponse {
    return {};
  },

  toJSON(_: RemoveFileResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RemoveFileResponse>): RemoveFileResponse {
    return RemoveFileResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RemoveFileResponse>): RemoveFileResponse {
    const message = createBaseRemoveFileResponse();
    return message;
  },
};

function createBaseMoveFileRequest(): MoveFileRequest {
  return { sandboxId: "", sourcePath: "", destinationPath: "" };
}

export const MoveFileRequest: MessageFns<MoveFileRequest> = {
  encode(message: MoveFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sandboxId !== "") {
      writer.uint32(10).string(message.sandboxId);
    }
    if (message.sourcePath !== "") {
      writer.uint32(18).string(message.sourcePath);
    }
    if (message.destinationPath !== "") {
      writer.uint32(26).string(message.destinationPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sandboxId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourcePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.destinationPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveFileRequest {
    return {
      sandboxId: isSet(object.sandboxId)
        ? globalThis.String(object.sandboxId)
        : isSet(object.sandbox_id)
        ? globalThis.String(object.sandbox_id)
        : "",
      sourcePath: isSet(object.sourcePath)
        ? globalThis.String(object.sourcePath)
        : isSet(object.source_path)
        ? globalThis.String(object.source_path)
        : "",
      destinationPath: isSet(object.destinationPath)
        ? globalThis.String(object.destinationPath)
        : isSet(object.destination_path)
        ? globalThis.String(object.destination_path)
        : "",
    };
  },

  toJSON(message: MoveFileRequest): unknown {
    const obj: any = {};
    if (message.sandboxId !== "") {
      obj.sandboxId = message.sandboxId;
    }
    if (message.sourcePath !== "") {
      obj.sourcePath = message.sourcePath;
    }
    if (message.destinationPath !== "") {
      obj.destinationPath = message.destinationPath;
    }
    return obj;
  },

  create(base?: DeepPartial<MoveFileRequest>): MoveFileRequest {
    return MoveFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MoveFileRequest>): MoveFileRequest {
    const message = createBaseMoveFileRequest();
    message.sandboxId = object.sandboxId ?? "";
    message.sourcePath = object.sourcePath ?? "";
    message.destinationPath = object.destinationPath ?? "";
    return message;
  },
};

function createBaseMoveFileResponse(): MoveFileResponse {
  return {};
}

export const MoveFileResponse: MessageFns<MoveFileResponse> = {
  encode(_: MoveFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MoveFileResponse {
    return {};
  },

  toJSON(_: MoveFileResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MoveFileResponse>): MoveFileResponse {
    return MoveFileResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MoveFileResponse>): MoveFileResponse {
    const message = createBaseMoveFileResponse();
    return message;
  },
};

function createBaseExecRequest(): ExecRequest {
  return { start: undefined, stdin: undefined, cancel: undefined, resize: undefined };
}

export const ExecRequest: MessageFns<ExecRequest> = {
  encode(message: ExecRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      ExecStart.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.stdin !== undefined) {
      StdinData.encode(message.stdin, writer.uint32(18).fork()).join();
    }
    if (message.cancel !== undefined) {
      ExecCancel.encode(message.cancel, writer.uint32(26).fork()).join();
    }
    if (message.resize !== undefined) {
      ExecResize.encode(message.resize, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = ExecStart.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stdin = StdinData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cancel = ExecCancel.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resize = ExecResize.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecRequest {
    return {
      start: isSet(object.start) ? ExecStart.fromJSON(object.start) : undefined,
      stdin: isSet(object.stdin) ? StdinData.fromJSON(object.stdin) : undefined,
      cancel: isSet(object.cancel) ? ExecCancel.fromJSON(object.cancel) : undefined,
      resize: isSet(object.resize) ? ExecResize.fromJSON(object.resize) : undefined,
    };
  },

  toJSON(message: ExecRequest): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = ExecStart.toJSON(message.start);
    }
    if (message.stdin !== undefined) {
      obj.stdin = StdinData.toJSON(message.stdin);
    }
    if (message.cancel !== undefined) {
      obj.cancel = ExecCancel.toJSON(message.cancel);
    }
    if (message.resize !== undefined) {
      obj.resize = ExecResize.toJSON(message.resize);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecRequest>): ExecRequest {
    return ExecRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecRequest>): ExecRequest {
    const message = createBaseExecRequest();
    message.start = (object.start !== undefined && object.start !== null)
      ? ExecStart.fromPartial(object.start)
      : undefined;
    message.stdin = (object.stdin !== undefined && object.stdin !== null)
      ? StdinData.fromPartial(object.stdin)
      : undefined;
    message.cancel = (object.cancel !== undefined && object.cancel !== null)
      ? ExecCancel.fromPartial(object.cancel)
      : undefined;
    message.resize = (object.resize !== undefined && object.resize !== null)
      ? ExecResize.fromPartial(object.resize)
      : undefined;
    return message;
  },
};

function createBaseExecResponse(): ExecResponse {
  return { stdout: undefined, stderr: undefined, exit: undefined };
}

export const ExecResponse: MessageFns<ExecResponse> = {
  encode(message: ExecResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stdout !== undefined) {
      StdoutData.encode(message.stdout, writer.uint32(10).fork()).join();
    }
    if (message.stderr !== undefined) {
      StderrData.encode(message.stderr, writer.uint32(18).fork()).join();
    }
    if (message.exit !== undefined) {
      ExecExit.encode(message.exit, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stdout = StdoutData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stderr = StderrData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.exit = ExecExit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecResponse {
    return {
      stdout: isSet(object.stdout) ? StdoutData.fromJSON(object.stdout) : undefined,
      stderr: isSet(object.stderr) ? StderrData.fromJSON(object.stderr) : undefined,
      exit: isSet(object.exit) ? ExecExit.fromJSON(object.exit) : undefined,
    };
  },

  toJSON(message: ExecResponse): unknown {
    const obj: any = {};
    if (message.stdout !== undefined) {
      obj.stdout = StdoutData.toJSON(message.stdout);
    }
    if (message.stderr !== undefined) {
      obj.stderr = StderrData.toJSON(message.stderr);
    }
    if (message.exit !== undefined) {
      obj.exit = ExecExit.toJSON(message.exit);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecResponse>): ExecResponse {
    return ExecResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecResponse>): ExecResponse {
    const message = createBaseExecResponse();
    message.stdout = (object.stdout !== undefined && object.stdout !== null)
      ? StdoutData.fromPartial(object.stdout)
      : undefined;
    message.stderr = (object.stderr !== undefined && object.stderr !== null)
      ? StderrData.fromPartial(object.stderr)
      : undefined;
    message.exit = (object.exit !== undefined && object.exit !== null) ? ExecExit.fromPartial(object.exit) : undefined;
    return message;
  },
};

function createBaseExecStart(): ExecStart {
  return { command: [], env: {}, workingDir: "", tty: false, cols: 0, rows: 0 };
}

export const ExecStart: MessageFns<ExecStart> = {
  encode(message: ExecStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.command) {
      writer.uint32(10).string(v!);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      ExecStart_EnvEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.workingDir !== "") {
      writer.uint32(26).string(message.workingDir);
    }
    if (message.tty !== false) {
      writer.uint32(32).bool(message.tty);
    }
    if (message.cols !== 0) {
      writer.uint32(40).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(48).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = ExecStart_EnvEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.env[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workingDir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tty = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecStart {
    return {
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      workingDir: isSet(object.workingDir)
        ? globalThis.String(object.workingDir)
        : isSet(object.working_dir)
        ? globalThis.String(object.working_dir)
        : "",
      tty: isSet(object.tty) ? globalThis.Boolean(object.tty) : false,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: ExecStart): unknown {
    const obj: any = {};
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.workingDir !== "") {
      obj.workingDir = message.workingDir;
    }
    if (message.tty !== false) {
      obj.tty = message.tty;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecStart>): ExecStart {
    return ExecStart.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecStart>): ExecStart {
    const message = createBaseExecStart();
    message.command = object.command?.map((e) => e) || [];
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.workingDir = object.workingDir ?? "";
    message.tty = object.tty ?? false;
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseExecStart_EnvEntry(): ExecStart_EnvEntry {
  return { key: "", value: "" };
}

export const ExecStart_EnvEntry: MessageFns<ExecStart_EnvEntry> = {
  encode(message: ExecStart_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecStart_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecStart_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecStart_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ExecStart_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ExecStart_EnvEntry>): ExecStart_EnvEntry {
    return ExecStart_EnvEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecStart_EnvEntry>): ExecStart_EnvEntry {
    const message = createBaseExecStart_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseStdinData(): StdinData {
  return { data: Buffer.alloc(0) };
}

export const StdinData: MessageFns<StdinData> = {
  encode(message: StdinData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StdinData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStdinData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StdinData {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: StdinData): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<StdinData>): StdinData {
    return StdinData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StdinData>): StdinData {
    const message = createBaseStdinData();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseStdoutData(): StdoutData {
  return { data: Buffer.alloc(0) };
}

export const StdoutData: MessageFns<StdoutData> = {
  encode(message: StdoutData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StdoutData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStdoutData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StdoutData {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: StdoutData): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<StdoutData>): StdoutData {
    return StdoutData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StdoutData>): StdoutData {
    const message = createBaseStdoutData();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseStderrData(): StderrData {
  return { data: Buffer.alloc(0) };
}

export const StderrData: MessageFns<StderrData> = {
  encode(message: StderrData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StderrData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStderrData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StderrData {
    return { data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0) };
  },

  toJSON(message: StderrData): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<StderrData>): StderrData {
    return StderrData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StderrData>): StderrData {
    const message = createBaseStderrData();
    message.data = object.data ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseExecCancel(): ExecCancel {
  return {};
}

export const ExecCancel: MessageFns<ExecCancel> = {
  encode(_: ExecCancel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecCancel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecCancel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ExecCancel {
    return {};
  },

  toJSON(_: ExecCancel): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ExecCancel>): ExecCancel {
    return ExecCancel.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ExecCancel>): ExecCancel {
    const message = createBaseExecCancel();
    return message;
  },
};

function createBaseExecResize(): ExecResize {
  return { cols: 0, rows: 0 };
}

export const ExecResize: MessageFns<ExecResize> = {
  encode(message: ExecResize, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cols !== 0) {
      writer.uint32(8).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(16).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecResize {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecResize();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecResize {
    return {
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: ExecResize): unknown {
    const obj: any = {};
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecResize>): ExecResize {
    return ExecResize.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecResize>): ExecResize {
    const message = createBaseExecResize();
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseExecExit(): ExecExit {
  return { exitCode: 0, signaled: false, signal: 0 };
}

export const ExecExit: MessageFns<ExecExit> = {
  encode(message: ExecExit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).int32(message.exitCode);
    }
    if (message.signaled !== false) {
      writer.uint32(16).bool(message.signaled);
    }
    if (message.signal !== 0) {
      writer.uint32(24).int32(message.signal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecExit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecExit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.signaled = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.signal = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecExit {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      signaled: isSet(object.signaled) ? globalThis.Boolean(object.signaled) : false,
      signal: isSet(object.signal) ? globalThis.Number(object.signal) : 0,
    };
  },

  toJSON(message: ExecExit): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.signaled !== false) {
      obj.signaled = message.signaled;
    }
    if (message.signal !== 0) {
      obj.signal = Math.round(message.signal);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecExit>): ExecExit {
    return ExecExit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecExit>): ExecExit {
    const message = createBaseExecExit();
    message.exitCode = object.exitCode ?? 0;
    message.signaled = object.signaled ?? false;
    message.signal = object.signal ?? 0;
    return message;
  },
};

function createBaseSessionInfo(): SessionInfo {
  return {
    name: "",
    command: [],
    workingDir: "",
    status: 0,
    attached: false,
    startedAtUnixNanos: 0,
    lastActivityUnixNanos: 0,
    endedAtUnixNanos: 0,
    exit: undefined,
  };
}

export const SessionInfo: MessageFns<SessionInfo> = {
  encode(message: SessionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.command) {
      writer.uint32(18).string(v!);
    }
    if (message.workingDir !== "") {
      writer.uint32(26).string(message.workingDir);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.attached !== false) {
      writer.uint32(40).bool(message.attached);
    }
    if (message.startedAtUnixNanos !== 0) {
      writer.uint32(48).int64(message.startedAtUnixNanos);
    }
    if (message.lastActivityUnixNanos !== 0) {
      writer.uint32(56).int64(message.lastActivityUnixNanos);
    }
    if (message.endedAtUnixNanos !== 0) {
      writer.uint32(64).int64(message.endedAtUnixNanos);
    }
    if (message.exit !== undefined) {
      ExecExit.encode(message.exit, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workingDir = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.attached = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.startedAtUnixNanos = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.lastActivityUnixNanos = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.endedAtUnixNanos = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.exit = ExecExit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      workingDir: isSet(object.workingDir)
        ? globalThis.String(object.workingDir)
        : isSet(object.working_dir)
        ? globalThis.String(object.working_dir)
        : "",
      status: isSet(object.status) ? sessionStatusFromJSON(object.status) : 0,
      attached: isSet(object.attached) ? globalThis.Boolean(object.attached) : false,
      startedAtUnixNanos: isSet(object.startedAtUnixNanos)
        ? globalThis.Number(object.startedAtUnixNanos)
        : isSet(object.started_at_unix_nanos)
        ? globalThis.Number(object.started_at_unix_nanos)
        : 0,
      lastActivityUnixNanos: isSet(object.lastActivityUnixNanos)
        ? globalThis.Number(object.lastActivityUnixNanos)
        : isSet(object.last_activity_unix_nanos)
        ? globalThis.Number(object.last_activity_unix_nanos)
        : 0,
      endedAtUnixNanos: isSet(object.endedAtUnixNanos)
        ? globalThis.Number(object.endedAtUnixNanos)
        : isSet(object.ended_at_unix_nanos)
        ? globalThis.Number(object.ended_at_unix_nanos)
        : 0,
      exit: isSet(object.exit) ? ExecExit.fromJSON(object.exit) : undefined,
    };
  },

  toJSON(message: SessionInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.workingDir !== "") {
      obj.workingDir = message.workingDir;
    }
    if (message.status !== 0) {
      obj.status = sessionStatusToJSON(message.status);
    }
    if (message.attached !== false) {
      obj.attached = message.attached;
    }
    if (message.startedAtUnixNanos !== 0) {
      obj.startedAtUnixNanos = Math.round(message.startedAtUnixNanos);
    }
    if (message.lastActivityUnixNanos !== 0) {
      obj.lastActivityUnixNanos = Math.round(message.lastActivityUnixNanos);
    }
    if (message.endedAtUnixNanos !== 0) {
      obj.endedAtUnixNanos = Math.round(message.endedAtUnixNanos);
    }
    if (message.exit !== undefined) {
      obj.exit = ExecExit.toJSON(message.exit);
    }
    return obj;
  },

  create(base?: DeepPartial<SessionInfo>): SessionInfo {
    return SessionInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionInfo>): SessionInfo {
    const message = createBaseSessionInfo();
    message.name = object.name ?? "";
    message.command = object.command?.map((e) => e) || [];
    message.workingDir = object.workingDir ?? "";
    message.status = object.status ?? 0;
    message.attached = object.attached ?? false;
    message.startedAtUnixNanos = object.startedAtUnixNanos ?? 0;
    message.lastActivityUnixNanos = object.lastActivityUnixNanos ?? 0;
    message.endedAtUnixNanos = object.endedAtUnixNanos ?? 0;
    message.exit = (object.exit !== undefined && object.exit !== null) ? ExecExit.fromPartial(object.exit) : undefined;
    return message;
  },
};

function createBaseCreateSessionRequest(): CreateSessionRequest {
  return { name: "", command: [], env: {}, workingDir: "", cols: 0, rows: 0, replace: false };
}

export const CreateSessionRequest: MessageFns<CreateSessionRequest> = {
  encode(message: CreateSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.command) {
      writer.uint32(18).string(v!);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      CreateSessionRequest_EnvEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.workingDir !== "") {
      writer.uint32(34).string(message.workingDir);
    }
    if (message.cols !== 0) {
      writer.uint32(40).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(48).uint32(message.rows);
    }
    if (message.replace !== false) {
      writer.uint32(56).bool(message.replace);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = CreateSessionRequest_EnvEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.env[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.workingDir = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.replace = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      command: globalThis.Array.isArray(object?.command) ? object.command.map((e: any) => globalThis.String(e)) : [],
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      workingDir: isSet(object.workingDir)
        ? globalThis.String(object.workingDir)
        : isSet(object.working_dir)
        ? globalThis.String(object.working_dir)
        : "",
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      replace: isSet(object.replace) ? globalThis.Boolean(object.replace) : false,
    };
  },

  toJSON(message: CreateSessionRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.workingDir !== "") {
      obj.workingDir = message.workingDir;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.replace !== false) {
      obj.replace = message.replace;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    return CreateSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionRequest>): CreateSessionRequest {
    const message = createBaseCreateSessionRequest();
    message.name = object.name ?? "";
    message.command = object.command?.map((e) => e) || [];
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.workingDir = object.workingDir ?? "";
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    message.replace = object.replace ?? false;
    return message;
  },
};

function createBaseCreateSessionRequest_EnvEntry(): CreateSessionRequest_EnvEntry {
  return { key: "", value: "" };
}

export const CreateSessionRequest_EnvEntry: MessageFns<CreateSessionRequest_EnvEntry> = {
  encode(message: CreateSessionRequest_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionRequest_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionRequest_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionRequest_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateSessionRequest_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionRequest_EnvEntry>): CreateSessionRequest_EnvEntry {
    return CreateSessionRequest_EnvEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionRequest_EnvEntry>): CreateSessionRequest_EnvEntry {
    const message = createBaseCreateSessionRequest_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateSessionResponse(): CreateSessionResponse {
  return { session: undefined };
}

export const CreateSessionResponse: MessageFns<CreateSessionResponse> = {
  encode(message: CreateSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      SessionInfo.encode(message.session, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = SessionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSessionResponse {
    return { session: isSet(object.session) ? SessionInfo.fromJSON(object.session) : undefined };
  },

  toJSON(message: CreateSessionResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = SessionInfo.toJSON(message.session);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    return CreateSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSessionResponse>): CreateSessionResponse {
    const message = createBaseCreateSessionResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? SessionInfo.fromPartial(object.session)
      : undefined;
    return message;
  },
};

function createBaseListSessionsRequest(): ListSessionsRequest {
  return {};
}

export const ListSessionsRequest: MessageFns<ListSessionsRequest> = {
  encode(_: ListSessionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSessionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSessionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListSessionsRequest {
    return {};
  },

  toJSON(_: ListSessionsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListSessionsRequest>): ListSessionsRequest {
    return ListSessionsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListSessionsRequest>): ListSessionsRequest {
    const message = createBaseListSessionsRequest();
    return message;
  },
};

function createBaseListSessionsResponse(): ListSessionsResponse {
  return { sessions: [], sandboxSuspended: false };
}

export const ListSessionsResponse: MessageFns<ListSessionsResponse> = {
  encode(message: ListSessionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sessions) {
      SessionInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.sandboxSuspended !== false) {
      writer.uint32(16).bool(message.sandboxSuspended);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSessionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSessionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessions.push(SessionInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sandboxSuspended = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSessionsResponse {
    return {
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => SessionInfo.fromJSON(e))
        : [],
      sandboxSuspended: isSet(object.sandboxSuspended)
        ? globalThis.Boolean(object.sandboxSuspended)
        : isSet(object.sandbox_suspended)
        ? globalThis.Boolean(object.sandbox_suspended)
        : false,
    };
  },

  toJSON(message: ListSessionsResponse): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => SessionInfo.toJSON(e));
    }
    if (message.sandboxSuspended !== false) {
      obj.sandboxSuspended = message.sandboxSuspended;
    }
    return obj;
  },

  create(base?: DeepPartial<ListSessionsResponse>): ListSessionsResponse {
    return ListSessionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListSessionsResponse>): ListSessionsResponse {
    const message = createBaseListSessionsResponse();
    message.sessions = object.sessions?.map((e) => SessionInfo.fromPartial(e)) || [];
    message.sandboxSuspended = object.sandboxSuspended ?? false;
    return message;
  },
};

function createBaseKillSessionRequest(): KillSessionRequest {
  return { name: "", signal: "", graceMs: 0 };
}

export const KillSessionRequest: MessageFns<KillSessionRequest> = {
  encode(message: KillSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.signal !== "") {
      writer.uint32(18).string(message.signal);
    }
    if (message.graceMs !== 0) {
      writer.uint32(24).uint32(message.graceMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signal = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.graceMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillSessionRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      signal: isSet(object.signal) ? globalThis.String(object.signal) : "",
      graceMs: isSet(object.graceMs)
        ? globalThis.Number(object.graceMs)
        : isSet(object.grace_ms)
        ? globalThis.Number(object.grace_ms)
        : 0,
    };
  },

  toJSON(message: KillSessionRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.signal !== "") {
      obj.signal = message.signal;
    }
    if (message.graceMs !== 0) {
      obj.graceMs = Math.round(message.graceMs);
    }
    return obj;
  },

  create(base?: DeepPartial<KillSessionRequest>): KillSessionRequest {
    return KillSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KillSessionRequest>): KillSessionRequest {
    const message = createBaseKillSessionRequest();
    message.name = object.name ?? "";
    message.signal = object.signal ?? "";
    message.graceMs = object.graceMs ?? 0;
    return message;
  },
};

function createBaseKillSessionResponse(): KillSessionResponse {
  return { session: undefined };
}

export const KillSessionResponse: MessageFns<KillSessionResponse> = {
  encode(message: KillSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      SessionInfo.encode(message.session, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = SessionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillSessionResponse {
    return { session: isSet(object.session) ? SessionInfo.fromJSON(object.session) : undefined };
  },

  toJSON(message: KillSessionResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = SessionInfo.toJSON(message.session);
    }
    return obj;
  },

  create(base?: DeepPartial<KillSessionResponse>): KillSessionResponse {
    return KillSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KillSessionResponse>): KillSessionResponse {
    const message = createBaseKillSessionResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? SessionInfo.fromPartial(object.session)
      : undefined;
    return message;
  },
};

function createBaseAttachSessionRequest(): AttachSessionRequest {
  return { start: undefined, stdin: undefined, resize: undefined, detach: undefined, signal: undefined };
}

export const AttachSessionRequest: MessageFns<AttachSessionRequest> = {
  encode(message: AttachSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      AttachStart.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.stdin !== undefined) {
      StdinData.encode(message.stdin, writer.uint32(18).fork()).join();
    }
    if (message.resize !== undefined) {
      ExecResize.encode(message.resize, writer.uint32(26).fork()).join();
    }
    if (message.detach !== undefined) {
      AttachDetach.encode(message.detach, writer.uint32(34).fork()).join();
    }
    if (message.signal !== undefined) {
      SessionSignal.encode(message.signal, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = AttachStart.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stdin = StdinData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resize = ExecResize.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.detach = AttachDetach.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signal = SessionSignal.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachSessionRequest {
    return {
      start: isSet(object.start) ? AttachStart.fromJSON(object.start) : undefined,
      stdin: isSet(object.stdin) ? StdinData.fromJSON(object.stdin) : undefined,
      resize: isSet(object.resize) ? ExecResize.fromJSON(object.resize) : undefined,
      detach: isSet(object.detach) ? AttachDetach.fromJSON(object.detach) : undefined,
      signal: isSet(object.signal) ? SessionSignal.fromJSON(object.signal) : undefined,
    };
  },

  toJSON(message: AttachSessionRequest): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = AttachStart.toJSON(message.start);
    }
    if (message.stdin !== undefined) {
      obj.stdin = StdinData.toJSON(message.stdin);
    }
    if (message.resize !== undefined) {
      obj.resize = ExecResize.toJSON(message.resize);
    }
    if (message.detach !== undefined) {
      obj.detach = AttachDetach.toJSON(message.detach);
    }
    if (message.signal !== undefined) {
      obj.signal = SessionSignal.toJSON(message.signal);
    }
    return obj;
  },

  create(base?: DeepPartial<AttachSessionRequest>): AttachSessionRequest {
    return AttachSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttachSessionRequest>): AttachSessionRequest {
    const message = createBaseAttachSessionRequest();
    message.start = (object.start !== undefined && object.start !== null)
      ? AttachStart.fromPartial(object.start)
      : undefined;
    message.stdin = (object.stdin !== undefined && object.stdin !== null)
      ? StdinData.fromPartial(object.stdin)
      : undefined;
    message.resize = (object.resize !== undefined && object.resize !== null)
      ? ExecResize.fromPartial(object.resize)
      : undefined;
    message.detach = (object.detach !== undefined && object.detach !== null)
      ? AttachDetach.fromPartial(object.detach)
      : undefined;
    message.signal = (object.signal !== undefined && object.signal !== null)
      ? SessionSignal.fromPartial(object.signal)
      : undefined;
    return message;
  },
};

function createBaseAttachStart(): AttachStart {
  return { name: "", cols: 0, rows: 0, maxReplayBytes: 0 };
}

export const AttachStart: MessageFns<AttachStart> = {
  encode(message: AttachStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.cols !== 0) {
      writer.uint32(16).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(24).uint32(message.rows);
    }
    if (message.maxReplayBytes !== 0) {
      writer.uint32(32).uint32(message.maxReplayBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxReplayBytes = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachStart {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      maxReplayBytes: isSet(object.maxReplayBytes)
        ? globalThis.Number(object.maxReplayBytes)
        : isSet(object.max_replay_bytes)
        ? globalThis.Number(object.max_replay_bytes)
        : 0,
    };
  },

  toJSON(message: AttachStart): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.maxReplayBytes !== 0) {
      obj.maxReplayBytes = Math.round(message.maxReplayBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<AttachStart>): AttachStart {
    return AttachStart.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttachStart>): AttachStart {
    const message = createBaseAttachStart();
    message.name = object.name ?? "";
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    message.maxReplayBytes = object.maxReplayBytes ?? 0;
    return message;
  },
};

function createBaseAttachDetach(): AttachDetach {
  return {};
}

export const AttachDetach: MessageFns<AttachDetach> = {
  encode(_: AttachDetach, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachDetach {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachDetach();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AttachDetach {
    return {};
  },

  toJSON(_: AttachDetach): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AttachDetach>): AttachDetach {
    return AttachDetach.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AttachDetach>): AttachDetach {
    const message = createBaseAttachDetach();
    return message;
  },
};

function createBaseSessionSignal(): SessionSignal {
  return { signal: "" };
}

export const SessionSignal: MessageFns<SessionSignal> = {
  encode(message: SessionSignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signal !== "") {
      writer.uint32(10).string(message.signal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionSignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionSignal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signal = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionSignal {
    return { signal: isSet(object.signal) ? globalThis.String(object.signal) : "" };
  },

  toJSON(message: SessionSignal): unknown {
    const obj: any = {};
    if (message.signal !== "") {
      obj.signal = message.signal;
    }
    return obj;
  },

  create(base?: DeepPartial<SessionSignal>): SessionSignal {
    return SessionSignal.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SessionSignal>): SessionSignal {
    const message = createBaseSessionSignal();
    message.signal = object.signal ?? "";
    return message;
  },
};

function createBaseAttachSessionResponse(): AttachSessionResponse {
  return { accepted: undefined, output: undefined, exit: undefined, ended: undefined, outputDropped: undefined };
}

export const AttachSessionResponse: MessageFns<AttachSessionResponse> = {
  encode(message: AttachSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== undefined) {
      AttachAccepted.encode(message.accepted, writer.uint32(10).fork()).join();
    }
    if (message.output !== undefined) {
      StdoutData.encode(message.output, writer.uint32(18).fork()).join();
    }
    if (message.exit !== undefined) {
      ExecExit.encode(message.exit, writer.uint32(26).fork()).join();
    }
    if (message.ended !== undefined) {
      AttachEnded.encode(message.ended, writer.uint32(34).fork()).join();
    }
    if (message.outputDropped !== undefined) {
      OutputDropped.encode(message.outputDropped, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accepted = AttachAccepted.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.output = StdoutData.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.exit = ExecExit.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ended = AttachEnded.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.outputDropped = OutputDropped.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachSessionResponse {
    return {
      accepted: isSet(object.accepted) ? AttachAccepted.fromJSON(object.accepted) : undefined,
      output: isSet(object.output) ? StdoutData.fromJSON(object.output) : undefined,
      exit: isSet(object.exit) ? ExecExit.fromJSON(object.exit) : undefined,
      ended: isSet(object.ended) ? AttachEnded.fromJSON(object.ended) : undefined,
      outputDropped: isSet(object.outputDropped)
        ? OutputDropped.fromJSON(object.outputDropped)
        : isSet(object.output_dropped)
        ? OutputDropped.fromJSON(object.output_dropped)
        : undefined,
    };
  },

  toJSON(message: AttachSessionResponse): unknown {
    const obj: any = {};
    if (message.accepted !== undefined) {
      obj.accepted = AttachAccepted.toJSON(message.accepted);
    }
    if (message.output !== undefined) {
      obj.output = StdoutData.toJSON(message.output);
    }
    if (message.exit !== undefined) {
      obj.exit = ExecExit.toJSON(message.exit);
    }
    if (message.ended !== undefined) {
      obj.ended = AttachEnded.toJSON(message.ended);
    }
    if (message.outputDropped !== undefined) {
      obj.outputDropped = OutputDropped.toJSON(message.outputDropped);
    }
    return obj;
  },

  create(base?: DeepPartial<AttachSessionResponse>): AttachSessionResponse {
    return AttachSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttachSessionResponse>): AttachSessionResponse {
    const message = createBaseAttachSessionResponse();
    message.accepted = (object.accepted !== undefined && object.accepted !== null)
      ? AttachAccepted.fromPartial(object.accepted)
      : undefined;
    message.output = (object.output !== undefined && object.output !== null)
      ? StdoutData.fromPartial(object.output)
      : undefined;
    message.exit = (object.exit !== undefined && object.exit !== null) ? ExecExit.fromPartial(object.exit) : undefined;
    message.ended = (object.ended !== undefined && object.ended !== null)
      ? AttachEnded.fromPartial(object.ended)
      : undefined;
    message.outputDropped = (object.outputDropped !== undefined && object.outputDropped !== null)
      ? OutputDropped.fromPartial(object.outputDropped)
      : undefined;
    return message;
  },
};

function createBaseOutputDropped(): OutputDropped {
  return { droppedBytes: 0 };
}

export const OutputDropped: MessageFns<OutputDropped> = {
  encode(message: OutputDropped, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.droppedBytes !== 0) {
      writer.uint32(8).uint64(message.droppedBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutputDropped {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutputDropped();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.droppedBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OutputDropped {
    return {
      droppedBytes: isSet(object.droppedBytes)
        ? globalThis.Number(object.droppedBytes)
        : isSet(object.dropped_bytes)
        ? globalThis.Number(object.dropped_bytes)
        : 0,
    };
  },

  toJSON(message: OutputDropped): unknown {
    const obj: any = {};
    if (message.droppedBytes !== 0) {
      obj.droppedBytes = Math.round(message.droppedBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<OutputDropped>): OutputDropped {
    return OutputDropped.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OutputDropped>): OutputDropped {
    const message = createBaseOutputDropped();
    message.droppedBytes = object.droppedBytes ?? 0;
    return message;
  },
};

function createBaseAttachAccepted(): AttachAccepted {
  return { session: undefined, replayedBytes: 0, historyDropped: false, cols: 0, rows: 0 };
}

export const AttachAccepted: MessageFns<AttachAccepted> = {
  encode(message: AttachAccepted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      SessionInfo.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.replayedBytes !== 0) {
      writer.uint32(16).uint32(message.replayedBytes);
    }
    if (message.historyDropped !== false) {
      writer.uint32(24).bool(message.historyDropped);
    }
    if (message.cols !== 0) {
      writer.uint32(32).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(40).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachAccepted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachAccepted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = SessionInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.replayedBytes = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.historyDropped = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachAccepted {
    return {
      session: isSet(object.session) ? SessionInfo.fromJSON(object.session) : undefined,
      replayedBytes: isSet(object.replayedBytes)
        ? globalThis.Number(object.replayedBytes)
        : isSet(object.replayed_bytes)
        ? globalThis.Number(object.replayed_bytes)
        : 0,
      historyDropped: isSet(object.historyDropped)
        ? globalThis.Boolean(object.historyDropped)
        : isSet(object.history_dropped)
        ? globalThis.Boolean(object.history_dropped)
        : false,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: AttachAccepted): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = SessionInfo.toJSON(message.session);
    }
    if (message.replayedBytes !== 0) {
      obj.replayedBytes = Math.round(message.replayedBytes);
    }
    if (message.historyDropped !== false) {
      obj.historyDropped = message.historyDropped;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create(base?: DeepPartial<AttachAccepted>): AttachAccepted {
    return AttachAccepted.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttachAccepted>): AttachAccepted {
    const message = createBaseAttachAccepted();
    message.session = (object.session !== undefined && object.session !== null)
      ? SessionInfo.fromPartial(object.session)
      : undefined;
    message.replayedBytes = object.replayedBytes ?? 0;
    message.historyDropped = object.historyDropped ?? false;
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseAttachEnded(): AttachEnded {
  return { reason: 0 };
}

export const AttachEnded: MessageFns<AttachEnded> = {
  encode(message: AttachEnded, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttachEnded {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttachEnded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttachEnded {
    return { reason: isSet(object.reason) ? attachEnded_ReasonFromJSON(object.reason) : 0 };
  },

  toJSON(message: AttachEnded): unknown {
    const obj: any = {};
    if (message.reason !== 0) {
      obj.reason = attachEnded_ReasonToJSON(message.reason);
    }
    return obj;
  },

  create(base?: DeepPartial<AttachEnded>): AttachEnded {
    return AttachEnded.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttachEnded>): AttachEnded {
    const message = createBaseAttachEnded();
    message.reason = object.reason ?? 0;
    return message;
  },
};

export type GuestServiceService = typeof GuestServiceService;
export const GuestServiceService = {
  health: {
    path: "/tyto.runtime.v1.GuestService/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
  postRestore: {
    path: "/tyto.runtime.v1.GuestService/PostRestore" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PostRestoreRequest): Buffer => Buffer.from(PostRestoreRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PostRestoreRequest => PostRestoreRequest.decode(value),
    responseSerialize: (value: PostRestoreResponse): Buffer => Buffer.from(PostRestoreResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PostRestoreResponse => PostRestoreResponse.decode(value),
  },
  prepareSuspend: {
    path: "/tyto.runtime.v1.GuestService/PrepareSuspend" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PrepareSuspendRequest): Buffer =>
      Buffer.from(PrepareSuspendRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PrepareSuspendRequest => PrepareSuspendRequest.decode(value),
    responseSerialize: (value: PrepareSuspendResponse): Buffer =>
      Buffer.from(PrepareSuspendResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PrepareSuspendResponse => PrepareSuspendResponse.decode(value),
  },
  abortSuspend: {
    path: "/tyto.runtime.v1.GuestService/AbortSuspend" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AbortSuspendRequest): Buffer => Buffer.from(AbortSuspendRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AbortSuspendRequest => AbortSuspendRequest.decode(value),
    responseSerialize: (value: AbortSuspendResponse): Buffer =>
      Buffer.from(AbortSuspendResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AbortSuspendResponse => AbortSuspendResponse.decode(value),
  },
  prepareResume: {
    path: "/tyto.runtime.v1.GuestService/PrepareResume" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PrepareResumeRequest): Buffer => Buffer.from(PrepareResumeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PrepareResumeRequest => PrepareResumeRequest.decode(value),
    responseSerialize: (value: PrepareResumeResponse): Buffer =>
      Buffer.from(PrepareResumeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PrepareResumeResponse => PrepareResumeResponse.decode(value),
  },
  freezeFilesystem: {
    path: "/tyto.runtime.v1.GuestService/FreezeFilesystem" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: FreezeFilesystemRequest): Buffer =>
      Buffer.from(FreezeFilesystemRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): FreezeFilesystemRequest => FreezeFilesystemRequest.decode(value),
    responseSerialize: (value: FreezeFilesystemResponse): Buffer =>
      Buffer.from(FreezeFilesystemResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): FreezeFilesystemResponse => FreezeFilesystemResponse.decode(value),
  },
  thawFilesystem: {
    path: "/tyto.runtime.v1.GuestService/ThawFilesystem" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ThawFilesystemRequest): Buffer =>
      Buffer.from(ThawFilesystemRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ThawFilesystemRequest => ThawFilesystemRequest.decode(value),
    responseSerialize: (value: ThawFilesystemResponse): Buffer =>
      Buffer.from(ThawFilesystemResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ThawFilesystemResponse => ThawFilesystemResponse.decode(value),
  },
  readFile: {
    path: "/tyto.runtime.v1.GuestService/ReadFile" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ReadFileRequest): Buffer => Buffer.from(ReadFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReadFileRequest => ReadFileRequest.decode(value),
    responseSerialize: (value: ReadFileResponse): Buffer => Buffer.from(ReadFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReadFileResponse => ReadFileResponse.decode(value),
  },
  writeFile: {
    path: "/tyto.runtime.v1.GuestService/WriteFile" as const,
    requestStream: true as const,
    responseStream: false as const,
    requestSerialize: (value: WriteFileRequest): Buffer => Buffer.from(WriteFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WriteFileRequest => WriteFileRequest.decode(value),
    responseSerialize: (value: WriteFileResponse): Buffer => Buffer.from(WriteFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): WriteFileResponse => WriteFileResponse.decode(value),
  },
  listDirectory: {
    path: "/tyto.runtime.v1.GuestService/ListDirectory" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ListDirectoryRequest): Buffer => Buffer.from(ListDirectoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListDirectoryRequest => ListDirectoryRequest.decode(value),
    responseSerialize: (value: ListDirectoryResponse): Buffer =>
      Buffer.from(ListDirectoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListDirectoryResponse => ListDirectoryResponse.decode(value),
  },
  statFile: {
    path: "/tyto.runtime.v1.GuestService/StatFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: StatFileRequest): Buffer => Buffer.from(StatFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StatFileRequest => StatFileRequest.decode(value),
    responseSerialize: (value: StatFileResponse): Buffer => Buffer.from(StatFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): StatFileResponse => StatFileResponse.decode(value),
  },
  makeDirectory: {
    path: "/tyto.runtime.v1.GuestService/MakeDirectory" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: MakeDirectoryRequest): Buffer => Buffer.from(MakeDirectoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): MakeDirectoryRequest => MakeDirectoryRequest.decode(value),
    responseSerialize: (value: MakeDirectoryResponse): Buffer =>
      Buffer.from(MakeDirectoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): MakeDirectoryResponse => MakeDirectoryResponse.decode(value),
  },
  removeFile: {
    path: "/tyto.runtime.v1.GuestService/RemoveFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RemoveFileRequest): Buffer => Buffer.from(RemoveFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RemoveFileRequest => RemoveFileRequest.decode(value),
    responseSerialize: (value: RemoveFileResponse): Buffer => Buffer.from(RemoveFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RemoveFileResponse => RemoveFileResponse.decode(value),
  },
  moveFile: {
    path: "/tyto.runtime.v1.GuestService/MoveFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: MoveFileRequest): Buffer => Buffer.from(MoveFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): MoveFileRequest => MoveFileRequest.decode(value),
    responseSerialize: (value: MoveFileResponse): Buffer => Buffer.from(MoveFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): MoveFileResponse => MoveFileResponse.decode(value),
  },
  exec: {
    path: "/tyto.runtime.v1.GuestService/Exec" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: ExecRequest): Buffer => Buffer.from(ExecRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecRequest => ExecRequest.decode(value),
    responseSerialize: (value: ExecResponse): Buffer => Buffer.from(ExecResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecResponse => ExecResponse.decode(value),
  },
  /**
   * Managed sessions (Sprint 14). Distinct from transient Exec: a session
   * outlives the client connection and is identified by name, not by
   * stream lifetime. See CreateSessionRequest.name for the identity rule.
   */
  createSession: {
    path: "/tyto.runtime.v1.GuestService/CreateSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateSessionRequest): Buffer => Buffer.from(CreateSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateSessionRequest => CreateSessionRequest.decode(value),
    responseSerialize: (value: CreateSessionResponse): Buffer =>
      Buffer.from(CreateSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateSessionResponse => CreateSessionResponse.decode(value),
  },
  listSessions: {
    path: "/tyto.runtime.v1.GuestService/ListSessions" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSessionsRequest): Buffer => Buffer.from(ListSessionsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSessionsRequest => ListSessionsRequest.decode(value),
    responseSerialize: (value: ListSessionsResponse): Buffer =>
      Buffer.from(ListSessionsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSessionsResponse => ListSessionsResponse.decode(value),
  },
  killSession: {
    path: "/tyto.runtime.v1.GuestService/KillSession" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: KillSessionRequest): Buffer => Buffer.from(KillSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): KillSessionRequest => KillSessionRequest.decode(value),
    responseSerialize: (value: KillSessionResponse): Buffer => Buffer.from(KillSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): KillSessionResponse => KillSessionResponse.decode(value),
  },
  attachSession: {
    path: "/tyto.runtime.v1.GuestService/AttachSession" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: AttachSessionRequest): Buffer => Buffer.from(AttachSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AttachSessionRequest => AttachSessionRequest.decode(value),
    responseSerialize: (value: AttachSessionResponse): Buffer =>
      Buffer.from(AttachSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AttachSessionResponse => AttachSessionResponse.decode(value),
  },
} as const;

export interface GuestServiceServer extends UntypedServiceImplementation {
  health: handleUnaryCall<HealthRequest, HealthResponse>;
  postRestore: handleUnaryCall<PostRestoreRequest, PostRestoreResponse>;
  prepareSuspend: handleUnaryCall<PrepareSuspendRequest, PrepareSuspendResponse>;
  abortSuspend: handleUnaryCall<AbortSuspendRequest, AbortSuspendResponse>;
  prepareResume: handleUnaryCall<PrepareResumeRequest, PrepareResumeResponse>;
  freezeFilesystem: handleUnaryCall<FreezeFilesystemRequest, FreezeFilesystemResponse>;
  thawFilesystem: handleUnaryCall<ThawFilesystemRequest, ThawFilesystemResponse>;
  readFile: handleServerStreamingCall<ReadFileRequest, ReadFileResponse>;
  writeFile: handleClientStreamingCall<WriteFileRequest, WriteFileResponse>;
  listDirectory: handleServerStreamingCall<ListDirectoryRequest, ListDirectoryResponse>;
  statFile: handleUnaryCall<StatFileRequest, StatFileResponse>;
  makeDirectory: handleUnaryCall<MakeDirectoryRequest, MakeDirectoryResponse>;
  removeFile: handleUnaryCall<RemoveFileRequest, RemoveFileResponse>;
  moveFile: handleUnaryCall<MoveFileRequest, MoveFileResponse>;
  exec: handleBidiStreamingCall<ExecRequest, ExecResponse>;
  /**
   * Managed sessions (Sprint 14). Distinct from transient Exec: a session
   * outlives the client connection and is identified by name, not by
   * stream lifetime. See CreateSessionRequest.name for the identity rule.
   */
  createSession: handleUnaryCall<CreateSessionRequest, CreateSessionResponse>;
  listSessions: handleUnaryCall<ListSessionsRequest, ListSessionsResponse>;
  killSession: handleUnaryCall<KillSessionRequest, KillSessionResponse>;
  attachSession: handleBidiStreamingCall<AttachSessionRequest, AttachSessionResponse>;
}

export interface GuestServiceClient extends Client {
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  postRestore(
    request: PostRestoreRequest,
    callback: (error: ServiceError | null, response: PostRestoreResponse) => void,
  ): ClientUnaryCall;
  postRestore(
    request: PostRestoreRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PostRestoreResponse) => void,
  ): ClientUnaryCall;
  postRestore(
    request: PostRestoreRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PostRestoreResponse) => void,
  ): ClientUnaryCall;
  prepareSuspend(
    request: PrepareSuspendRequest,
    callback: (error: ServiceError | null, response: PrepareSuspendResponse) => void,
  ): ClientUnaryCall;
  prepareSuspend(
    request: PrepareSuspendRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PrepareSuspendResponse) => void,
  ): ClientUnaryCall;
  prepareSuspend(
    request: PrepareSuspendRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PrepareSuspendResponse) => void,
  ): ClientUnaryCall;
  abortSuspend(
    request: AbortSuspendRequest,
    callback: (error: ServiceError | null, response: AbortSuspendResponse) => void,
  ): ClientUnaryCall;
  abortSuspend(
    request: AbortSuspendRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AbortSuspendResponse) => void,
  ): ClientUnaryCall;
  abortSuspend(
    request: AbortSuspendRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AbortSuspendResponse) => void,
  ): ClientUnaryCall;
  prepareResume(
    request: PrepareResumeRequest,
    callback: (error: ServiceError | null, response: PrepareResumeResponse) => void,
  ): ClientUnaryCall;
  prepareResume(
    request: PrepareResumeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PrepareResumeResponse) => void,
  ): ClientUnaryCall;
  prepareResume(
    request: PrepareResumeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PrepareResumeResponse) => void,
  ): ClientUnaryCall;
  freezeFilesystem(
    request: FreezeFilesystemRequest,
    callback: (error: ServiceError | null, response: FreezeFilesystemResponse) => void,
  ): ClientUnaryCall;
  freezeFilesystem(
    request: FreezeFilesystemRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: FreezeFilesystemResponse) => void,
  ): ClientUnaryCall;
  freezeFilesystem(
    request: FreezeFilesystemRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: FreezeFilesystemResponse) => void,
  ): ClientUnaryCall;
  thawFilesystem(
    request: ThawFilesystemRequest,
    callback: (error: ServiceError | null, response: ThawFilesystemResponse) => void,
  ): ClientUnaryCall;
  thawFilesystem(
    request: ThawFilesystemRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ThawFilesystemResponse) => void,
  ): ClientUnaryCall;
  thawFilesystem(
    request: ThawFilesystemRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ThawFilesystemResponse) => void,
  ): ClientUnaryCall;
  readFile(request: ReadFileRequest, options?: Partial<CallOptions>): ClientReadableStream<ReadFileResponse>;
  readFile(
    request: ReadFileRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ReadFileResponse>;
  writeFile(
    callback: (error: ServiceError | null, response: WriteFileResponse) => void,
  ): ClientWritableStream<WriteFileRequest>;
  writeFile(
    metadata: Metadata,
    callback: (error: ServiceError | null, response: WriteFileResponse) => void,
  ): ClientWritableStream<WriteFileRequest>;
  writeFile(
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: WriteFileResponse) => void,
  ): ClientWritableStream<WriteFileRequest>;
  writeFile(
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: WriteFileResponse) => void,
  ): ClientWritableStream<WriteFileRequest>;
  listDirectory(
    request: ListDirectoryRequest,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ListDirectoryResponse>;
  listDirectory(
    request: ListDirectoryRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ListDirectoryResponse>;
  statFile(
    request: StatFileRequest,
    callback: (error: ServiceError | null, response: StatFileResponse) => void,
  ): ClientUnaryCall;
  statFile(
    request: StatFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StatFileResponse) => void,
  ): ClientUnaryCall;
  statFile(
    request: StatFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StatFileResponse) => void,
  ): ClientUnaryCall;
  makeDirectory(
    request: MakeDirectoryRequest,
    callback: (error: ServiceError | null, response: MakeDirectoryResponse) => void,
  ): ClientUnaryCall;
  makeDirectory(
    request: MakeDirectoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: MakeDirectoryResponse) => void,
  ): ClientUnaryCall;
  makeDirectory(
    request: MakeDirectoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: MakeDirectoryResponse) => void,
  ): ClientUnaryCall;
  removeFile(
    request: RemoveFileRequest,
    callback: (error: ServiceError | null, response: RemoveFileResponse) => void,
  ): ClientUnaryCall;
  removeFile(
    request: RemoveFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RemoveFileResponse) => void,
  ): ClientUnaryCall;
  removeFile(
    request: RemoveFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RemoveFileResponse) => void,
  ): ClientUnaryCall;
  moveFile(
    request: MoveFileRequest,
    callback: (error: ServiceError | null, response: MoveFileResponse) => void,
  ): ClientUnaryCall;
  moveFile(
    request: MoveFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: MoveFileResponse) => void,
  ): ClientUnaryCall;
  moveFile(
    request: MoveFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: MoveFileResponse) => void,
  ): ClientUnaryCall;
  exec(): ClientDuplexStream<ExecRequest, ExecResponse>;
  exec(options: Partial<CallOptions>): ClientDuplexStream<ExecRequest, ExecResponse>;
  exec(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<ExecRequest, ExecResponse>;
  /**
   * Managed sessions (Sprint 14). Distinct from transient Exec: a session
   * outlives the client connection and is identified by name, not by
   * stream lifetime. See CreateSessionRequest.name for the identity rule.
   */
  createSession(
    request: CreateSessionRequest,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  createSession(
    request: CreateSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  createSession(
    request: CreateSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateSessionResponse) => void,
  ): ClientUnaryCall;
  listSessions(
    request: ListSessionsRequest,
    callback: (error: ServiceError | null, response: ListSessionsResponse) => void,
  ): ClientUnaryCall;
  listSessions(
    request: ListSessionsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSessionsResponse) => void,
  ): ClientUnaryCall;
  listSessions(
    request: ListSessionsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSessionsResponse) => void,
  ): ClientUnaryCall;
  killSession(
    request: KillSessionRequest,
    callback: (error: ServiceError | null, response: KillSessionResponse) => void,
  ): ClientUnaryCall;
  killSession(
    request: KillSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: KillSessionResponse) => void,
  ): ClientUnaryCall;
  killSession(
    request: KillSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: KillSessionResponse) => void,
  ): ClientUnaryCall;
  attachSession(): ClientDuplexStream<AttachSessionRequest, AttachSessionResponse>;
  attachSession(options: Partial<CallOptions>): ClientDuplexStream<AttachSessionRequest, AttachSessionResponse>;
  attachSession(
    metadata: Metadata,
    options?: Partial<CallOptions>,
  ): ClientDuplexStream<AttachSessionRequest, AttachSessionResponse>;
}

export const GuestServiceClient = makeGenericClientConstructor(
  GuestServiceService,
  "tyto.runtime.v1.GuestService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GuestServiceClient;
  service: typeof GuestServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
